#include "types.h"
#include "stat.h"
#include "user.h"

int main(int argc, char* argv[])
{

    // make the message() system call
    // worldpeace();
    
    worldpeace();
    printf(1,"\n");
    exit();
}